/* 특수문자 */

/* 문법
 * 특수문자는 \', \", \?, \\, \n, \t 등이 있습니다.
 *
 * 의미
 * 순서대로 작은 따옴표, 큰 따옴표, 물음표, 역슬래시, 줄바꿈, 탭을 가리킵니다. 
 */

int i = 0; /* To Do: i의 초기값을 문자 '\''에 해당하는 정수값으로 설정해보세요 */
int j = 0; /* To Do: j의 초기값을 문자 '\\'에 해당하는 정수값으로 설정해보세요 */
int k = 0; /* To Do: k의 초기값을 문자 '\n'에 해당하는 정수값으로 설정해보세요 */

int main () {
   return 0;
}
